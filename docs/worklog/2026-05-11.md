# 2026-05-11 工作日志

项目：BlockProof 区块链电子文件存证系统

---

## 改动记录

1. **ipfs_service.py** — 同步 `open()` 改 `aiofiles` 异步读
2. **blockchain_service.py** — 合约调用加上重试，RPC 抽风自动退避重试
3. **main.py** — SQLite 改 FastAPI Depends 注入；新增 POST /batch-upload 批量上传
4. **models.py** — BatchItemResult / BatchUploadResponse 数据模型
5. **EvidenceStorage.sol** — OwnershipTransferred 事件；_allHashes 数组改为固定容量环形缓冲 + 事件溯源
6. **App.js** — 轮询可配 + 钱包兼容 + Toast 通知 + Dark/Light 主题
7. **ErrorBoundary.jsx** — 新增错误边界
8. **Navbar.jsx** — 钱包错误提示 + 主题切换按钮
9. **UploadDropzone.jsx** — 多文件批量上传，自动切单/批量模式
10. **Toast.jsx** — 新增 Toast 通知组件（success/error/info）
11. **Skeleton.jsx** — 新增骨架屏组件
12. **Dashboard.jsx** — 加载时骨架屏占位
13. **auth.py** — 新增 API 认证，x-api-key header 校验
14. **tests/test_hash.py** — pytest 测试 SM3/SHA-256 哈希确定性
15. **.github/workflows/test.yml** — GitHub Actions CI 自动跑 pytest
16. **start.sh** — 改为 anvil 起链 + venv 后端 + npm 前端
17. **STARTUP_GUIDE.md** — 启动文档
18. **NIXOS_DEBUG.md** — NixOS 环境调试记录
