# 2026-05-15 工作日志

项目：BlockProof — 区块链电子文件存证系统

## 核心改动

### 后端
- **SQL 语法修复**：`/admin/dashboard` 的 SQL 字符串引号冲突，`DATE("now")` 改成 `DATE('now')`
- **device_id 存证数继承**：旧记录 uploader="anonymous"，按 device_id 查不到。修复为 device_id 用户直接显示链上总数，不走 DB 筛选
- **设备总数修复**：MiniStat 设备数不再依赖 `device_stats` 的 key 数（仅 Top 5），新增 `total_device_count` 字段返回全量去重数
- **dashboard 设备 Top 5**：`most_common(10)` 改回 `most_common(5)`，排序天然从大到小

### 前端
- **门户页自动刷新**：`onEnter={() => setHasDeviceId(true)}` 改为 `window.location.reload()`，进门后页面刷新，所有状态全新
- **使用率计算**：分母从 10000（导致 0%）改为合约 MAX_RECENT=200，18 条记录显示 9%
- **AdminConsole 设备数**：改用 `dashData?.total_device_count`

## 文件改动

| 文件 | 改动 |
|------|------|
| `backend/main.py` | SQL 引号修复、stats 兼容 device_id、dashboard 加 total_device_count、most_common 5 |
| `backend/blockchain_service.py` | `get_contract_stats` 非地址跳过链上查询 |
| `frontend/src/App.js` | 进门户改为 `window.location.reload()` |
| `frontend/src/components/AdminConsole.jsx` | 设备数用 `total_device_count`、使用率分母 200 |

## 已知问题
1. device_id 清缓存后变化，后台设备数会持续增长
2. 沙箱无法重启宿主机服务
3. admin 默认账密 admin / admin123，生产必须改
# 2026-05-15 工作日志（下）

项目：BlockProof — 区块链电子文件存证系统

## 核心改动

### 后端
- **/stats 重构**：device_id 作为临时账户，按密钥精确统计 DB 存证数。换密钥 → 新账户 → 显示 0
- **dashboard 设备 Top 5**：`most_common(10)` 改回 5，排序从大到小
- **使用率分母修正**：10000 → 合约 MAX_RECENT=200，18 条记录显示 9%
- **total_device_count**：新增字段，返回全量去重设备数，不限于 Top 5

### 前端
- **门户页自动刷新**：`onEnter` 改为 `window.location.reload()`，进门后全状态更新
- **AdminConsole 设备数**：用 `dashData?.total_device_count` 替代 `Object.keys().length`

## 文件改动

| 文件 | 改动 |
|------|------|
| `backend/main.py` | stats 按 uploader 精确计数、dashboard total_device_count、most_common 5 |
| `frontend/src/App.js` | 进门户改为 `window.location.reload()` |
| `frontend/src/components/AdminConsole.jsx` | 设备数用 `total_device_count`、使用率分母 200 |

## 最终架构决策
- device_id = 临时账户，按 key 统计，换 key = 换账户
- 钱包地址 = 正式身份，走区块链查询
- 无身份 → your_evidence_count = 0